name: QA Portfolio CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  placeholder:
    name: Portfolio under restructure - CI being rebuilt per project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI is being rewired project-by-project as the 7-folder structure lands. See commit history."
