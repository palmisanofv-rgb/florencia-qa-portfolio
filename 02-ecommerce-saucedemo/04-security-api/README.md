# Security & API Testing

Swag Labs has no public backend API to test — see [`security-checks.md`](security-checks.md) for the basic security pass performed instead (response headers, and the fact that access control here is entirely client-side).
